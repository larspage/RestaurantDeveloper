# Project-specific MCP Environment Variables
# These override the global settings for this project

# PostgreSQL MCP Database (project-specific)
MCP_POSTGRES_URL=postgresql://mcp_user:mcp_secure_password@localhost:5432/mcp_mindmap_project
MCP_PG_DATABASE=mcp_mindmap_project

# MongoDB MCP Database (project-specific)
MCP_MONGODB_URI=mongodb://localhost:27017/mcp_mindmap_project
MCP_MONGO_DATABASE=mcp_mindmap_project

# Project directory for filesystem MCP
MCP_PROJECT_DIR=/absolute/path/to/your/current/project

# GitHub token (can use global or project-specific)
MCP_GITHUB_TOKEN=your_project_specific_github_token_here